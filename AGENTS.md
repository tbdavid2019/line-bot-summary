# Agent 協作與開發規範 (AGENTS.md)

本文件定義 AI Agent 在此專案中進行開發、維護與協作時必須嚴格遵守的規範。

---

## 📌 核心準則與主動文檔規範 (Proactive Documentation Rules)

### 1. 異動主動記錄 CHANGELOG.md（強制）
- **原則**：每一次對專案的實質異動（包含新增功能、修復錯誤、重構、依賴調整、設定更新等），**必須主動**更新專案根目錄下的 [`CHANGELOG.md`](CHANGELOG.md)。
- **記錄格式**：
  - 日期（YYYY-MM-DD）與變更類型（如 `Added`, `Changed`, `Fixed`, `Deprecated`, `Removed`, `Security`）。
  - 清晰扼要地說明異動內容與影響範圍。

### 2. 視情況主動更新 README.md（強制）
- **原則**：若異動涉及以下情境，**必須主動**同步更新 [`README.md`](README.md)：
  - 新增/調整使用者功能或對話指令。
  - 新增/調整環境變數（`.env`）或設定檔。
  - 修改安裝依賴（`requirements.txt` / `Dockerfile`）或部署流程。
  - 系統架構或重要支援平台有所變更。
- **要求**：確保文檔隨時反映最新程式碼狀態，避免使用者依循過期說明操作。

---

## 🛠️ 開發與維護工作流程
1. **執行前確認**：每次操作前確認遠端同步狀態（例如 `git pull`），檢查最新程式碼與依賴。
2. **執行中驗證**：遵循零幻覺與即時檢索原則，保持代碼簡潔並確保向後相容。
3. **完成後紀錄**：任務完成前**主動檢查並補齊** `CHANGELOG.md` 與 `README.md` 的更新，無須使用者重複提醒。
